import { Container } from '@/components/layout/Container';

export function CartPage() {
  return (
    <Container className="py-12">
      <p className="text-text-muted text-center">Cart page — coming soon</p>
    </Container>
  );
}
